# ✅ 驗證部署並修復 404

## 🎯 快速檢查步驟

### 步驟 1: 確認訪問正確的 URL

**Preview URL**（用於測試）：
```
https://ichen-i3lr89uxl-leos-projects-0a2aae41.vercel.app/
```

**Production URL**（正式環境）：
```
https://ichen-app.vercel.app/
```
或
```
https://your-custom-domain.com/
```

**如何找到 Production URL**：
1. 前往 Vercel Dashboard → 你的專案
2. 點擊 **Deployments** 標籤
3. 找到狀態為 **Production** 的部署
4. 點擊該部署，查看 URL

---

### 步驟 2: 檢查 Vercel 專案設定

前往 **Settings** → **General**，確認以下設定：

#### ✅ 必須正確的設定

| 設定項目 | 正確值 | 你的設定 |
|---------|--------|---------|
| **Root Directory** | `apps/home` | ✅ 已設定 |
| **Framework Preset** | `Next.js` | 應該自動偵測 |
| **Build Command** | `pnpm build` | ⚠️ 需要確認 |
| **Output Directory** | `.next` 或留空 | ⚠️ 需要確認 |
| **Install Command** | `cd ../.. && pnpm install` | ⚠️ 需要確認 |

#### 🔧 Build Command 設定

**選項 1（推薦，如果 Root Directory 已設定）**：
```
pnpm build
```

**選項 2（如果選項 1 不行）**：
```
cd ../.. && pnpm build --filter=home
```

---

### 步驟 3: 檢查部署狀態

1. 前往 **Deployments** 標籤
2. 確認最新部署的狀態：
   - ✅ **Ready**（綠色）= 部署成功
   - ⚠️ **Building** = 正在建置，請等待
   - ❌ **Error** = 建置失敗，查看錯誤訊息

---

### 步驟 4: 清除緩存並重新部署

如果設定都正確但還是 404：

1. 前往 **Deployments** 標籤
2. 點擊最新部署右側的 **⋯** → **Redeploy**
3. **取消勾選** "Use existing Build Cache"
4. 點擊 **Redeploy**
5. 等待建置完成

---

## 🔍 診斷 404 的常見原因

### 原因 1: Build Command 不正確

**症狀**：建置成功但訪問時 404

**解決方案**：
- 確認 Build Command 為 `pnpm build`（如果 Root Directory 是 `apps/home`）
- 或使用 `cd ../.. && pnpm build --filter=home`

### 原因 2: Output Directory 設定錯誤

**症狀**：建置成功但找不到輸出文件

**解決方案**：
- 將 Output Directory 設為 `.next`
- 或留空（Next.js 會自動處理）

### 原因 3: 訪問了錯誤的 URL

**症狀**：Preview URL 正常但 Production URL 404

**解決方案**：
- 確認訪問的是 Production URL，不是 Preview URL
- 檢查 Vercel Dashboard → Deployments → 找到 Production 部署的 URL

### 原因 4: 部署尚未完成

**症狀**：建置日誌顯示成功但訪問時 404

**解決方案**：
- 等待幾分鐘讓部署完全生效
- 清除瀏覽器緩存後重試
- 使用無痕模式訪問

---

## 🚀 推薦的完整設定

### 對於 `apps/home` 應用：

```
Root Directory: apps/home
Framework Preset: Next.js
Build Command: pnpm build
Output Directory: .next
Install Command: cd ../.. && pnpm install
Development Command: cd ../.. && pnpm dev --filter=home
```

---

## 📞 如果還是無法解決

請提供以下資訊：

1. **Vercel Dashboard 截圖**：
   - Settings → General（顯示所有設定）
   - Deployments（顯示最新部署狀態）

2. **建置日誌**：
   - 完整的建置日誌（特別是路由生成部分）

3. **訪問的 URL**：
   - 你實際訪問的 URL
   - 是 Preview 還是 Production？

4. **瀏覽器錯誤訊息**：
   - 開發者工具（F12）→ Console 和 Network 標籤的錯誤訊息

